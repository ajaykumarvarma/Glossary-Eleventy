---
layout: base
title: Glossary
---

{% block content %}
<h1 class="text-4xl font-bold mb-6 text-center">{{ title }}</h1>
<dl class="grid grid-cols-1 gap-4 place-items-center mb-8">
  {% for item in glossary %}
    <div class="max-w-xl w-full p-4 bg-white rounded-lg shadow-md transition duration-500 ease-in-out transform hover:-translate-y-1 hover:scale-110">
      <dt class="font-bold text-xl text-blue-500">{{ item.term }}</dt>
      <dd class="text-gray-600">{{ item.definition }}</dd>
    </div>
  {% endfor %}
</dl>
{% endblock %}

